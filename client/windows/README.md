# Windows client

This directory is the ownership boundary for the future Windows client and its
installer source. Windows implementation is intentionally frozen during LAN V1;
no executable or placeholder client is claimed as delivered.

When implementation starts, source stays here and final `.exe` or `.msi`
artifacts are published under `release/windows/`.
